---
title: <oelt-branching>
description: Branching scenario component — declarative decision points whose paths validation can prove reachable and completable.
---

:::caution[Draft]
Stub pending the `<oelt-branching>` spec in [oeltkit/oeltkit](https://github.com/oeltkit/oeltkit). Nothing below is filled in with guesses.
:::

A decision point that routes learners down different paths. Because the branch structure is declarative, validation can prove every path is reachable and completable.

## Markup

> TODO: canonical markup sample (the exact element an LLM writes) — pending the component spec.

## Attributes

> TODO: pending the component spec.

## Keyboard map

> TODO: pending the component's a11y test report.

## Screen-reader behavior

> TODO: pending the component's a11y test report.

## Tracking events

> TODO: pending the tracking spec.
