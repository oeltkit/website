---
title: MCP setup per client
description: Connect the OELTKit MCP server to Claude Desktop, Cowork, or Claude Code so your assistant can scaffold, validate, and package courses.
sidebar:
  order: 4
---

:::caution[Draft]
This page is a stub. The MCP server package name, tool list, and exact config snippets are **TODO** pending the MCP spec in [oeltkit/oeltkit](https://github.com/oeltkit/oeltkit).
:::

The OELTKit MCP server gives your AI assistant direct tools to scaffold courses, run validation, and build packages — no copy-pasting CLI output into chat.

## Claude Desktop

> TODO: config snippet for `claude_desktop_config.json` — pending the MCP server package.

## Cowork

> TODO: connection steps — pending the MCP server package.

## Claude Code

> TODO: `claude mcp add` command — pending the MCP server package.

## Tool list

> TODO: the MCP tools the server exposes (and which CLI commands they wrap) — pending the MCP spec.
